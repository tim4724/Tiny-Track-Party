node_modules/
.playwright-mcp/
test-results/
*.log
.DS_Store
# Audition candidate dumps — 760 MB of rejected sound candidates that were
# force-added once and then shipped in the Docker image for months:
#   _stk-audition/    STK SFX (mixed CC-BY-SA/GPL/CC0)
#   _engine-audition/ OpenGameArt engine clips (mostly CC0)
#   _music-pool/      race-music candidates (all CC-BY 4.0)
# They now live ONLY on the `audio-auditions` branch, which also carries the
# galleries that browse them (/sfx-audition.html and friends) and
# scripts/build-music-pool.mjs. Do NOT `git add -f` them back onto main: branch
# from audio-auditions for the next round, then ship only the promoted files.
public/assets/audio/_stk-audition/
public/assets/audio/_engine-audition/
public/assets/audio/_music-pool/
# Re-fetchable third-party bytes, each behind a URL + sha256 and a script that
# rebuilds it. They are a cache, not a source:
#   music-masters/ the 256-320 kbps incompetech downloads the shipped VBR files
#                  are encoded from (~164 MB) — `npm run fetch:music`
#   kenney-kits/   the three CC0 kits the game's models are picked from, for the
#                  asset gallery's kit browser (~30 MB) — `npm run fetch:kits`
.cache/
# Local-only Claude Code state — keeps the .claude/ root committable for shared
# project settings/hooks while excluding per-user runtime files (worktrees,
# local settings, scheduler lock).
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
# Sibling git worktrees checked out INSIDE the repo, which this tree is worked
# in many of. Each is another branch of this same repository, so it is local by
# definition — there is nothing here a remote could want. Anchored, so a
# `worktrees/` that means something else deeper in the tree is unaffected.
/worktrees/
native/build/
# Trailer renders — frames, per-shot clips and the cut master (a 4K/60 master runs to
# hundreds of MB). Wholly derived from scripts/trailer/shots.js, and the shot seeds are
# pinned there, so `npm run trailer` reproduces the same film: the bytes are an output,
# not a source. Publish the master from a release, don't commit it.
artwork/trailer/

# Local scratch from the authoring/preview scripts (preview-tracks.mjs writes
# its gallery beside itself; captures land wherever they are pointed).
scripts/.preview-tracks.html
*.jpeg
# One-off probes. A throwaway that drives Playwright has to live INSIDE the repo
# — ESM resolves from the file's own directory, so a script in the system temp
# dir cannot import @playwright/test — and the ones written at the root have
# repeatedly outlived their session and then failed `npm run lint`. This is
# where they go instead; eslint.config.js ignores it for the same reason.
scratch/

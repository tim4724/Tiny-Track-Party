// Textured matte ground — the biome's floor canvas.
//
// The JS ground is a plane wearing a tiled 256² canvas texture (lawn mowing
// stripes, sand grit, red-rock strata, snow drift, wood planks) on a Lambert
// material with a white tint: the texture carries ALL of the colour. The port
// approximated it with vertical vertex-colour bands, which is right for a
// stripe pattern and wrong for anything with structure in both axes — the
// playroom's wood floor lost its plank seams, end joints and knots and read as
// flat diagonal banding.
//
// So: same texture, generated pixel-for-pixel in C++ and sampled here, on the
// same hand-written Lambert as vlit (see that file for why the lighting is
// spelled out instead of using Filament's `lit` model). flipUV is off so the
// C++ UVs (world XZ / tile) map straight through.
//
// THE LIGHTING IS ttp_shade.inc's, not a copy of it. This file carried its own
// transcription of sunVisibility, kPenumbraWorld and the ambient/sun terms —
// ninety lines, in step with the shared include only because both were edited
// together every time (the uvToRenderTargetUV fix had to land twice). That is
// the exact failure ttp_shade.inc's header says it exists to prevent, and it was
// the last copy left.
//
// The ground DOES catch the sun's baked map, and it also CASTS into it. The
// thing the original opt-out was protecting against is a CAR's ground blob
// detaching onto the lawn far below its deck — but that blob is a conformed
// decal (vdecal), a different mechanism entirely.
//
// Casting looks pointless for a flat floor (it throws nothing on itself) and is
// not: outside every caster the depth map holds its clear value, the far plane,
// and a receiver's soft-min then compares against THAT rather than against its
// own surface. It is the difference between a penumbra and a hard edge — see
// buildTrackScene, where it was measured.
material {
    name : TtpVground,
    requires : [
        uv0,
        // the world normal the Lambert term needs (getWorldGeometricNormalVector)
        tangents
    ],
    shadingModel : unlit,
    culling : none,
    doubleSided : true,
    flipUV : false,
    parameters : [
        { type : sampler2d, name : albedo },
        // The frozen sun map, shared with vlit — same space, same compare.
        { type : sampler2d, name : shadowMap, precision : high },
        { type : mat4, name : shadowFromWorld, precision : high },
        { type : float, name : shadowTexel },
        { type : float, name : shadowK },
        // 1 / ortho depth range: turns a world-unit bias into normalised depth.
        { type : float, name : shadowDepthScale }
    ]
}

fragment {
    #include "ttp_shade.inc"

    void material(inout MaterialInputs material) {
        prepareMaterial(material);
        // Unlit adds `emissive` to `baseColor`; all the shading goes in the
        // former so nothing else in the pipeline touches it.
        material.baseColor = vec4(0.0, 0.0, 0.0, 1.0);
        // The ground canvas is the biome's baked lawn/sand/snow texture; the
        // shading over it is vlit's, term for term.
        material.emissive = vec4(ttpMatteShade(
                texture(materialParams_albedo, getUV0()).rgb,
                getWorldGeometricNormalVector()), 0.0);
    }
}

// The 2D cell overlay: the split-screen dividers and the per-player steer bar.
//
// WHY THE RENDERER DRAWS THESE AT ALL. architecture.md keeps the HUD in the
// shells because the Sticker Bash look is a UI-TOOLKIT problem — variable-font
// shaping, sticker chrome, transitions — and Filament is not one. The line that
// admits exactly these two and nothing else:
//
//     cell-anchored AND textless -> renderer
//     anything carrying type or sticker chrome -> shell
//
// A rounded rect with a translating fill has no type in it, is pinned to a cell
// whose geometry is already C++ (ttp_grid_cell), and is the ONLY HUD element
// that wants every frame — so moving it leaves the whole remaining HUD a ~6 Hz
// poll, which is a far cleaner contract for three shells than each writing its
// own 60 Hz animation over a GL surface. The place ordinal, the name chip, the
// item slot, the FINISHED card and the reconnect QR all stay in the shell.
//
// ONE MATERIAL, TWO SHAPES, because the whole bar composites in one fragment:
//   border == 0  a plain rect in `ink` — a divider rule
//   border >  0  the steer bar: ink border ring, `surface` interior, the 40 %
//                centre tick, then the livery fill, in that paint order
// Doing it in one pass is not (only) about draw calls: .cell-steer's children
// composite over KNOWN opaque colours, so the tick's 40 % and the fill's edges
// can be mixed here in sRGB — exactly the arithmetic the browser did — instead
// of being blended against the framebuffer in whatever space it happens to be.
//
// COLOUR SPACE. This draws AFTER the present pass (see TtpRenderer::drawOverlay),
// straight onto the swap chain, with post-processing off — so a value written
// here IS the on-panel sRGB value, and the only real framebuffer blend left (the
// divider's 0.88 and the shapes' antialiased edges) blends sRGB against sRGB,
// like CSS compositing. Authoring in the scene's linear space and letting
// vpresent grade it would blend in LINEAR instead, which is a visibly different
// answer: ink at 40 % over white is 0.67 in sRGB and 0.81 in linear.
//
// Units are DEVICE pixels throughout, scaled from CSS/point sizes by the
// frame's uiScale. Every px-valued parameter and the varying carry it at high
// precision: a mediump float has ~4 px of slack at 4 K, which would smear a
// 4 px rule.
material {
    name : TtpVoverlay,
    parameters : [
        // the quad's own size, so the vertex stage can hand the fragment real
        // pixels rather than a 0..1 corner
        { type : float2, name : size,    precision : high },
        // x radius, y border width, z centre-tick width (all device px)
        { type : float3, name : shape,   precision : high },
        // the moving fill: x left edge (local px), y width, z corner radius
        { type : float3, name : fill,    precision : high },
        { type : float4, name : ink },     // --ink, with the divider's alpha in w
        { type : float3, name : surface }, // --surface
        { type : float3, name : bar }      // --c: this player's livery
    ],
    variables : [ uv ],
    shadingModel : unlit,
    blending : transparent,
    culling : none,
    depthWrite : false,
    depthCulling : false
}

vertex {
    void materialVertex(inout MaterialVertexInputs material) {
        // The mesh is a unit quad; the transform scales it to `size`. Carrying
        // the local PIXEL coordinate is what lets every measurement below be a
        // CSS number straight off display.css.
        material.uv.xy = getPosition().xy * materialParams.size;
    }
}

fragment {
    // Signed distance to a rounded box centred on the origin. r is clamped to
    // the half-extent, which is also CSS's rule for an over-large radius: a
    // `border-radius: 999px` pill on a 34 px-tall bar resolves to 17.
    float sdBox(highp vec2 p, highp vec2 h, highp float r) {
        r = clamp(r, 0.0, min(h.x, h.y));
        highp vec2 q = abs(p) - h + r;
        return length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0) - r;
    }

    // Pixel coverage from a distance in pixels: a one-texel ramp across the
    // edge, which is the antialiasing the browser's rasterizer gave the DOM
    // bar. No fwidth() needed — the varying IS in pixels, so |grad d| == 1.
    float cov(highp float d) { return clamp(0.5 - d, 0.0, 1.0); }

    void material(inout MaterialInputs material) {
        prepareMaterial(material);
        highp vec2 h = materialParams.size * 0.5;
        highp vec2 p = variable_uv.xy - h;
        highp float border = materialParams.shape.y;

        float a = cov(sdBox(p, h, materialParams.shape.x)) * materialParams.ink.w;
        vec3 c = materialParams.ink.rgb;

        if (border > 0.0) {
            // .cell-steer: 4 px ink border, --surface interior, overflow hidden.
            // The interior's corner radius is the outer one less the border,
            // which is how CSS curves an inner edge.
            float inner = cov(sdBox(p, h - border, materialParams.shape.x - border));
            c = mix(c, materialParams.surface, inner);

            // .cell-steer::before — a 3 px rule at the padding box's midpoint
            // (left: 50%), ink at 40 % over the surface it sits on. It paints
            // BEFORE the fill and is covered by it wherever they overlap, which
            // is most of the travel: the fill is half the inner width, centred.
            highp float x = variable_uv.x;
            float tick = clamp(min(x - h.x, h.x + materialParams.shape.z - x) + 0.5,
                    0.0, 1.0) * inner;
            c = mix(c, mix(materialParams.surface, materialParams.ink.rgb, 0.4), tick);

            // .cell-steer__fill — the pill that tracks raw phone tilt, clipped
            // to the interior by `inner` (the parent's overflow: hidden).
            highp vec2 fh = vec2(materialParams.fill.y * 0.5, h.y - border);
            highp vec2 fc = vec2(materialParams.fill.x + fh.x, h.y);
            c = mix(c, materialParams.bar,
                    cov(sdBox(variable_uv.xy - fc, fh, materialParams.fill.z)) * inner);
        }

        // Premultiplied, like vblend: Filament's `transparent` blending works in
        // premultiplied alpha, so a straight-alpha colour would add its full
        // value at the antialiased edges and turn every curve into a hard one.
        material.baseColor = vec4(c * a, a);
    }
}
